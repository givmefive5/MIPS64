package service;

import java.util.List;

import exceptions.InvalidFormatException;

public class MIPS64Checker {

	public static List<String> parse(String text) throws InvalidFormatException {
		//read each line start from .data
		//parse the variables 	//variable name: .datatype size
		
		//until .text
		//parse the command		//operation: reg/label
		//generate instruction opcode
		throw new InvalidFormatException("Parsing error, unknown format");
	}
}
